import "print.uv";
import "print.uv"; // twice to check double importation behaviour

import "print.uv" as std; // check name-space access


fn main(): int {
	let a: int;
	let b: i64;
	let c: i64;

	a = 1;
	b = 2;

	std.println(a);
	println(b);

	c = a + b;
	println(c);

	return 0;
}