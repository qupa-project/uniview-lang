fn test(a: int, b: int) {
	if (a > b) {
		printf("%s", "Greater");
	} elif (a < b) {
		printf("%s", "Lesser");
	} elif (a > b) {
		printf("%s", "Logic error, should have already been caught");
	} else {
		printf("%s", "Equal");
	}

	return;
}


struct Test {}
fn UnifiedUndefined() {
	let t = Blank#[Test]();

	if (true) {
		delete t;
	} else {
		delete t;
	}
}

fn main(): int {
	test(1, 3);
	test(2, 2);
	test(3, 1);

	return 0;
}