fn main (): int {
	let a := 1;
	let b := 2;
	let c := 3.0;
	let d := 4.0;

	if (a + b != 3) {
		printf("%s", "Failed add");
		return 1;
	}
	if (a - b != -1) {
		printf("%s", "Failed subtract");
		return 1;
	}
	if (a * b != 2) {
		printf("%s", "Failed multiply");
		return 1;
	}
	if (a / b != 0) {
		printf("%s", "Failed divide");
		return 1;
	}
	if (a % b != 1) {
		printf("%s", "Failed modulo");
		return 1;
	}

	let res_float := c + d;
	res_float = c - d;
	res_float = c * d;
	res_float = c / d;
	res_float = c % d;

	return 0;
}