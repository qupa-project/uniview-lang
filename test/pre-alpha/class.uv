struct Cat {
	id: int;
}

impl Cat{
	fn New(id: int): Cat {
		let out = Blank#[Cat]();
		out.id = id;
		return out;
	}

	fn Say(this: Cat) {
		printf("%i: meow\n", this.id);
	}

	fn Say(this: @Cat) {
		printf("%i: meow\n", this.id);
	}

	fn Clone(src: @Cat): Cat {
		let out = Blank#[Cat]();
		printf("%s", "Cloning cat\n");
		out.id = src.id + 1;

		return out;
	}
}

impl Drop for Cat {
	fn drop(this: @Cat) {
		printf("%s", "Deleting cat...\n");
	}
}

fn main(): int {
	let c = Cat.New(2);
	Cat.Say($c);
	Cat.Say(@c);
	return 0;
}